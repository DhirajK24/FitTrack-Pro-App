org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m
android.useAndroidX=true
android.enableJetifier=true

# Disable incremental compilation to avoid path issues across drives
kotlin.incremental=false
org.gradle.caching=false

# Point to Java 17 installation
# Update this path to match your Java 17 installation location
# Try common Java 17 locations
# org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.9.9-hotspot
# Commented out - letting Gradle use system JAVA_HOME instead